import { motion } from "framer-motion";
import { Zap, Shield, TrendingUp, CornerRightUp } from "lucide-react";
import { useRef, useState } from "react";

// --- User-Defined Color Palette ---
const ACCENT = "#F2AE1D";   // Gold/Yellow
const PRIMARY_DARK = "#3A3B3A"; // Dark Text/Primary
const BACKGROUND = "#FAFAFA";   // Soft Background

// Data for the benefit modules (Concise Content)
const MODULES = [
    {
        icon: Shield,
        title: "Absolute Certainty",
        description: "Zero overruns. Guaranteed fixed timelines.",
        delay: 0.1,
    },
    {
        icon: TrendingUp,
        title: "Investment Maximized",
        description: "Advanced value engineering for peak market value.",
        delay: 0.2,
    },
    {
        icon: Zap,
        title: "Digital Transparency",
        description: "Real-time client portal and instant team access.",
        delay: 0.3,
    },
];

// Animation variants for staggered entrance
const containerVariants = {
    hidden: { opacity: 0 },
    visible: {
        opacity: 1,
        transition: {
            staggerChildren: 0.15,
            delayChildren: 0.4
        }
    }
};

const itemVariants = {
    hidden: { opacity: 0, x: -30 },
    visible: {
        opacity: 1,
        x: 0,
        transition: {
            duration: 0.7,
            ease: [0.25, 1, 0.5, 1],
        }
    },
};

const SculptedMultiLayerSection = () => {
    const [hoveredIndex, setHoveredIndex] = useState(null);
    const sectionRef = useRef(null);

    return (
        <section ref={sectionRef} className="w-full relative py-36 sm:py-52" style={{ background: BACKGROUND }}>

            {/* Background Texture Layer */}
            <div className="absolute inset-0 bg-[url('/noise.png')] opacity-5 pointer-events-none z-0"></div>

            <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 relative z-10">

                {/* --- Section Header (The part you love) --- */}
                <motion.div
                    initial={{ opacity: 0, y: -40 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    viewport={{ once: true, amount: 0.3 }}
                    transition={{ duration: 0.9, ease: [0.4, 0, 0.2, 1] }}
                    className="mb-24 max-w-4xl"
                >
                    <p style={{ color: ACCENT }} className="text-sm font-light uppercase tracking-[0.25em] mb-4">
                        The Quintessence of Development
                    </p>
                    <h3 style={{ color: PRIMARY_DARK }} className="text-5xl sm:text-7xl lg:text-8xl font-extralight leading-none tracking-tighter">
                        Delivering <span className="font-bold">Absolute Certainty</span> in Luxury Projects.
                    </h3>
                </motion.div>

                {/* --- CONTENT GRID: Main Statement + Modules --- */}
                <motion.div
                    variants={containerVariants}
                    initial="hidden"
                    whileInView="visible"
                    viewport={{ once: true, amount: 0.2 }}
                    className="grid lg:grid-cols-12 gap-10 lg:gap-16 items-start"
                >

                    {/* COLUMN 1: MAIN STATEMENT (The Dark Anchor Block) */}
                    <motion.div
                        variants={itemVariants}
                        className="lg:col-span-7 p-12 lg:p-16 rounded-3xl relative overflow-hidden shadow-2xl shadow-black/10"
                        style={{ backgroundColor: PRIMARY_DARK }} // High contrast anchor
                    >
                        {/* Gold Accent Corner (Sharp and High Contrast) */}
                        <div style={{ background: ACCENT }} className="absolute top-0 right-0 w-10 h-10 rounded-bl-xl"></div>

                        <div className="mb-6">
                            <CornerRightUp className="h-8 w-8" style={{ color: ACCENT }} />
                        </div>

                        <h4 style={{ color: ACCENT }} className="text-3xl lg:text-4xl font-semibold mb-6 leading-snug">
                            The Architects of Certainty
                        </h4>
                        <p className="text-white text-lg lg:text-xl text-opacity-80 leading-relaxed font-light max-w-prose">
                            Our bespoke process is an engineering blueprint for **risk-free luxury development**. We guarantee every milestone and control every cost, freeing you to focus solely on realizing your vision, without compromise.
                        </p>
                        <div className="mt-10 lg:mt-12">
                            <span style={{ color: ACCENT }} className="font-medium flex items-center gap-3 text-lg underline underline-offset-4">
                                Learn More About Our Commitment
                            </span>
                        </div>
                    </motion.div>

                    {/* COLUMN 2: WHAT YOU GET PANELS (Clean, Segmented List) */}
                    <div className="lg:col-span-5 relative space-y-6 lg:space-y-8">
                        {MODULES.map((module, index) => (
                            <motion.div
                                key={index}
                                variants={itemVariants}
                                onMouseEnter={() => setHoveredIndex(index)}
                                onMouseLeave={() => setHoveredIndex(null)}
                                className={`relative flex items-start gap-6 p-6 rounded-xl bg-white transition-all duration-300 shadow-lg shadow-black/5`}
                                style={{
                                    // Subtle lift on hover
                                    transform: hoveredIndex === index ? 'translateY(-3px)' : 'translateY(0)',
                                    // Accent line on the left, subtle thickness for luxury
                                    borderLeft: hoveredIndex === index ? `4px solid ${ACCENT}` : '4px solid transparent',
                                    // Ensure the item still has a solid border/definition
                                    border: '1px solid #eee'
                                }}
                            >
                                {/* --- Icon Container (Minimalist Square) --- */}
                                <div style={{ backgroundColor: ACCENT }} className="p-3 rounded-md shrink-0 mt-1 shadow-md">
                                    <module.icon className="h-5 w-5" style={{ color: PRIMARY_DARK }} />
                                </div>

                                {/* Content */}
                                <div>
                                    <h4 style={{ color: PRIMARY_DARK }} className="text-xl font-bold mb-1 leading-tight">
                                        {module.title}
                                    </h4>
                                    <p style={{ color: PRIMARY_DARK }} className="text-base text-opacity-70 font-light">
                                        {module.description}
                                    </p>
                                </div>
                            </motion.div>
                        ))}
                    </div>
                </motion.div>

                {/* Bottom CTA */}
                <motion.div
                    initial={{ opacity: 0, y: 50 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    viewport={{ once: true, amount: 0.3 }}
                    transition={{ duration: 0.8, delay: 1.0, ease: [0.4, 0, 0.2, 1] }}
                    className="mt-32 sm:mt-40 text-center"
                >
                    <button
                        style={{ backgroundColor: PRIMARY_DARK, borderColor: ACCENT }}
                        className="group inline-flex items-center justify-center gap-4 px-12 py-5 text-white rounded-full font-bold shadow-2xl shadow-black/30 transition-all duration-300 text-lg border-2 hover:bg-white hover:text-[#3A3B3A] hover:shadow-3xl hover:shadow-black/20"
                    >
                        Explore Full Service Details
                    </button>
                </motion.div>
            </div>
        </section>
    );
};

export default SculptedMultiLayerSection;
